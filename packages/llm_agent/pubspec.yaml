name: llm_agent
description: LLM agent framework with tool-calling — the AI Go teacher.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  go_board:
    path: ../go_board
  analysis_core:
    path: ../analysis_core

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
