name: sgf_parser
description: SGF (Smart Game Format) parser and writer for Go game records.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  go_board:
    path: ../go_board

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
